# ── Required ─────────────────────────────────────────────────────────────────
PORT=5000
FRONTEND_URL=http://localhost:5173

# ── After fine-tuning: set these to use your Phi-3 model ─────────────────────
# HF_MODEL_URL=https://api-inference.huggingface.co/models/YOUR_USERNAME/infraplan-phi3
# HF_TOKEN=hf_your_token_here

# Without HF_MODEL_URL the app uses the built-in rule-based engine (fully functional)
